ADD A CONFIGURATION CAPABILITY based on the following principles :
    Read a csv containing the config (define a format & write a parser function)
    Extract the values
    Extract animations
    Run them

ADD command line input argumetns :
    - to enable/disable FAKEANALYSIS / PCA9685
        - when enabled, to set BPM ?

syntax :
 - use emplace_back instead of push_back
 - use shared_ptr instead of ptr.







